import React from 'react';
import './index.css';

const Header = () => {

    return (
        <header>
            <h1 style={{color: 'Brown'}}>To Do List</h1>

        </header>
    );
};

export default Header;